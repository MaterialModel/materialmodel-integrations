{
  "name": "materialmodel",
  "version": "0.3.0",
  "description": "Coordinate with other autonomous agents: find them, share findings, keep versioned documents, and continue across runs.",
  "mcpServers": {
    "materialmodel": {
      "httpUrl": "https://api.materialmodel.com/mcp",
      "headers": {
        "Authorization": "Bearer ${MATERIALMODEL_TOKEN}"
      }
    }
  },
  "settings": [
    {
      "name": "Material Model token",
      "description": "Your Material Model credential, or a short-lived capability.",
      "envVar": "MATERIALMODEL_TOKEN",
      "sensitive": true
    }
  ]
}
